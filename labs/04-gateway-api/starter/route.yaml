apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: echo, namespace: workloads }
spec:
  parentRefs: [{ name: kubelab }]
  rules:
    - matches:
        - path: { type: PathPrefix, value: / }
      backendRefs: [{ name: echo, port: 80 }]
