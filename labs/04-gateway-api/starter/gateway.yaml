apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata: { name: kubelab, namespace: workloads }
spec:
  gatewayClassName: ${GATEWAY_CLASS}
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces: { from: Same }
